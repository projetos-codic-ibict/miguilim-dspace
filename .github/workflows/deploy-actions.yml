name: Deploy DSpace

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Executing Remote SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME_SSH }}
          password: ${{ secrets.PASSWORD_SSH }}
          script: |
            cd git && rm -rf miguilim-dspace
            mkdir miguilin-dspace && cd miguilin-dspace
            git clone http://${{ secrets.USERNAME_GIT }}:${{ secrets.PASSWORD_GIT }}@${{ secrets.REPOSITORY }} .
            sh docker/dspace/build-dspace.sh
            
